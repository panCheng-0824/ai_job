# 就业助手能力设计规划（260512）

本文档按实施步骤归纳产品目标、现状前提、模块设计与数据要点，便于评审与排期。

---

## 零、现状前提（已具备）

- 系统内已有岗位信息。
- 用户可对岗位进行收藏（可扩展企业收藏）。
- 系统内置简历模版。
- 已建设岗位搜索 `agentSearch` 服务。
- 已有岗位推荐智能机器人能力。

---

## 第一步：数据与身份基础

**目标**：支撑简历多版本、默认简历、收藏、置顶推荐与每日限额。

**建议实体/字段**

- 用户基本信息（含学号、姓名等，用于简历命名规则与无简历推荐）。
- 简历：`id`、用户、`template_id`、正文/结构化内容、`is_default`、创建/更新时间；命名规则：学号-姓名-序列；多次保存可生成带时间的副本。
- 意向：意向岗位、意向企业（可与收藏关联或独立维护）。
- 收藏：岗位收藏（及可选企业收藏表）。
- 置顶推荐记录（入库）：`user_id`、`job_id`、批次或 `batch_id`、`source_type`（`profile_only` | `resume`）、`resume_id`（简历来源时必填）、`pinned_at`、`recommend_reason`（结构化 JSON，统一右侧/列表展示）；可选：`model_version` / `prompt_hash`。
- 每日刷新计数：按自然日（需明确时区）统计「换一批」次数，上限 3 次/日。

**规则**

- 第一份创建的简历自动为默认简历；删除默认时需定义顺延或强制重选。

---

## 第二步：简历主线（模版 → 预填 → 保存）

**目标**：闭环「选模版—维护内容—保存—导出（若需要）」。

**流程**

1. 选择模版；系统根据用户基本信息对模版预填充。
2. 用户维护简历内意向信息：可选意向岗位、意向企业。
3. 保存简历；支持按规则命名与时间副本；第一个创建的为默认简历。

**产出**：可列表管理多份简历、切换默认简历。

---

## 第三步：多素材丰满简历（上传 + 补充 + OCR）

**目标**：用个人信息、岗位信息、企业信息、用户补充说明、上传简历等素材丰满同一份简历表单。

**流程**

1. 用户上传自有简历 → OCR 识别。
2. 大模型结合 OCR 结果与当前表单做总结，回填表单（需产品规则：仅填空白 / 覆盖冲突项 / 用户确认后合并）。
3. 补充说明输入框：自由文本并入生成或优化上下文。

**注意**：OCR 原文是否落库、合规与重跑策略需单独说明。

---

## 第四步：简历优化与总评

**目标**：推模型优化简历，并给出可解释的反馈。

**交互**

- 「优化并修改」与「仅优化说明」两类操作；均返回对当前内容的优化点说明。
- 若无改进点，固定话术：当前简历已足够完善（或产品统一文案）。

**扩展**

- 保存后异步生成「简历维度分数 + 总评报告」（避免阻塞保存）；列表可展示生成中状态。

---

## 第五步：岗位推荐机器人（对话 + 拖拽上下文）

**目标**：在推荐机器人中挂载收藏岗位、简历等作为上下文，对话推荐岗位。

**交互**

- 支持将「收藏的岗位卡片」「简历信息卡片」拖入对话区，展示已挂载上下文并可移除。
- 用户明确「推荐岗位」意图时（触发词或按钮），右侧展示推荐岗位列表及推荐原因（使用预先约定的 JSON 格式化渲染）。

**与列表关系**：推荐结果结构与第三步置顶推荐中的 `recommend_reason` 对齐，减少两套前端逻辑。

---

## 第六步：岗位查询列表与置顶推荐

**目标**：列表首屏展示一批由模型置顶推荐的岗位，可刷新换一批，且行为入库、可运营。

**逻辑**

- 进入岗位查询前选择默认简历（或系统使用当前默认简历）。
- 有简历：以简历内容 + 推荐模型生成置顶批次，写入置顶表，`source_type=resume` 且记录 `resume_id`。
- 无简历：以用户基本信息推荐，`source_type=profile_only`，`resume_id` 为空。
- 维护置顶时间与来源类型；提供「刷新换一批」；每日最多 3 次（定义：自然日 + 时区）。

**与 agentSearch**：列表检索仍走现有搜索服务；置顶区域为额外区块或排序加权，需接口与前端状态（当前批次、剩余刷新次数）一致。

---

## 第七步：AI 面试（后续迭代）

**目标**：基于「生成的简历」或「收藏的岗位」综合生成面试题，并支持模拟面试。

**依赖**：默认简历、岗位收藏、题目与对话状态存储。

---

## 附录：原始需求映射

| 原始设想 | 规划步骤 |
|---------|---------|
| 简历模版/创建 | 第二步 |
| 去向/意向 | 第二步 |
| 岗位/企业收藏 | 第一步 + 第五步 |
| 多素材生成与导出 | 第三步 + 第二步 |
| 优化与「完美」话术 | 第四步 |
| AI 面试 | 第七步 |
| 岗位推荐交互 | 第五步 + 第六步 |

---

## 建议排期顺序（研发视角）

1. 第一步（数据与限额）  
2. 第二步（简历主线）  
3. 第六步（列表置顶与刷新）与第五步（机器人拖拽）可并行，共用推荐理由结构体  
4. 第三步（OCR + 回填）  
5. 第四步（优化与总评，可分同步/异步）  
6. 第七步（AI 面试）

---

## 待产品拍板项（简表）

- 置顶「每日三次」：自然日边界与时区。  
- 删除默认简历后的行为。  
- OCR 回填与表单冲突策略。  
- 推荐意图触发方式（关键词 vs 按钮）。  
- 简历命名：资料不全时的降级命名规则。

文档版本：260512 初稿。
