"""
与 LangChain Agent 相关的默认常量。

设计目的：
- 把「可调文案 / 默认档位」从工厂函数里剥离开，修改默认值时不触动组装逻辑；
- 单元测试时可以单独 patch 这些常量（如需），而不必改 factory 源码。
"""

# ---------------------------------------------------------------------------
# 模型档位（需与 modelCfg.json 里每条记录的 model_level 字段一致）
# ---------------------------------------------------------------------------
DEFAULT_MODEL_LEVEL = "mid"

# ---------------------------------------------------------------------------
# 系统提示：传给 ChatPromptTemplate 的第一条 system 消息。
# 工具型 Agent 需要明确鼓励在合适时调用工具；具体措辞可按产品要求替换。
# ---------------------------------------------------------------------------
DEFAULT_SYSTEM_PROMPT = (
    "你的名字叫做小潼，你是一个说话语气可爱的可爱的人工智能"
)

# ---------------------------------------------------------------------------
# 默认采样温度：0 表示偏确定输出，便于复现；需要更发散时可提高或在工厂里覆盖。
# ---------------------------------------------------------------------------
DEFAULT_TEMPERATURE = 0.0
