
ai 面试官思考规划.
1.根据学生提供的岗位以及简历 进行 信息提取，最终返回结构化的题目大纲。可以抽离出来作为一个工具
    1.1 根据语义搜索 查找是否有相关题库，如果有则返回相关题库（返回的缓存题库 学生也可以选择重新规划），如果没有则进行重新规划
    1.2 重新规划 需要一个高级模型 根据学生简历的基本信息 以及岗位信息 进行题目编排。
    1.3 如果是重新规划的题目大纲，需要将编排后的题目持久化道本地。
    1.4 题目的大纲的结构设计要注意
    1.5 基于大纲为学生创建本地持久化面试记录表


2.做面试前数据准备
    2.1 获取面试题目参考大纲，当前学生的面试记录（进度）表
    2.2 获取当前已积累的用户画像数据资产

3.等待题目设计完毕之后，进入答题环节，根据面试记录表来判断面试进度
    3.1 开始自我介绍环节，这也是评分的一部分
    3.2 根据提供的基本信息（简历+岗位+题目大纲），以及当前的面试问题 创建子agent 进行题目大纲的深度思考来生成详细的面试题目
        3.2.1 这里有不断挖掘的思想，需要re-act的思想 直到本题挖掘结束 在记录表中进行评分写标注，这里对学生的意图也需要做特殊处理，例如 进行回答/对问题提出疑问/需要给出提示（只是提示不能给出答案 ）
        3.2.2 在挖掘的过程中不仅仅只关注纯技术方面，还需要结合 岗位信息和简历信息 问一些结合业务的问题，或者设计场景。（大纲是从岗位和简历提炼出来的，那么也可以结合具体业务）
        3.2.3 为每一道题目设计宽裕的时间，前端根据时间判断 如果用户没有给出答案，可以内部发出消息提示agent 给出提示 同时给出新的答复时间。（也是需要维护到记录表中的）
        3.2.4 如果给出了提示后学生还是没有给出答案 则认为放弃本题。
        3.2.5 如果在面试过程中 出现了关闭窗口 则认为 放弃本次面试，后台为本次面试相应的记录表进行维护，生成总结。
        3.2.6 可以根据面试记录进行重新面试。（考虑，在创建面试记录表的时候 直接将当时选择的岗位信息和企业信息也维护到 面试记录表中，避免重新面试的时候关联的岗位信息和企业信息发生变动）
        3.2.7 进行面试总结 生成报告（维护面试记录中的总结）

4.数据资产维护 通过异步的方式，。
    4.1 将入口放在维护面试记录
        4.1.1针对维护答题记录，可以积累面试官针对类似题型的一个提问经验
        4.1.2针对维护面试总结，可以积累学生群体画像资产

==================================================================================================================================================================================================================================



    这是一个非常出色的设计大纲，结构清晰，考虑周全。结合我们之前所有关于**Harness思想、状态机、产物契约、记忆锚点、异步Reflection**的讨论，我来帮你将它优化为一份更完整、更具工程落地指导意义的**技术设计蓝图**。

---

## 🏗️ 优化后的AI面试官设计蓝图

### **阶段〇：核心资产定义**

在开始之前，需要先定义几个贯穿全流程的核心数据结构：

| 资产名称 | 核心字段 | 作用 |
| :--- | :--- | :--- |
| **面试大纲** | `plan_id`, `version`, `questions[]`, `target_role`, `source_material_hash` | 结构化的题目集合，是“技能记忆”的核心载体 |
| **面试记录** | `session_id`, `user_id`, `plan_id` + `version`, `status`, `answers[]`, `started_at`, `completed_at` | 单次面试的完整状态与结果，支持断点续传 |
| **用户画像** | `user_id`, `strengths[]`, `weaknesses[]`, `communication_style`, `last_update` | 跨会话的持久记忆 |
| **人群洞察** | `insight_type`, `pattern`, `confidence`, `suggested_action` | 异步反思产出的群体智慧资产 |


### **阶段一：面试规划 (规划与资产准备)**

> 对应你的第1点。

```mermaid
flowchart TD
    A[学生提交<br>简历+岗位+说明] --> B[工具: 语义指纹提取]
    B --> C{语义缓存查询}
    C -- 命中 --> D[展示缓存大纲<br>生成时间/相似度/摘要]
    C -- 未命中 --> E[调用高级模型<br>生成全新大纲]
    D --> F{学生决策}
    F -- 接受缓存 --> H
    F -- 修改/拒绝 --> E
    E --> G[生成结构化大纲<br>符合产物契约]
    G --> H[持久化大纲到本地DB<br>记录source_material_hash]
    H --> I[创建面试记录表<br>初始化session状态]
```

**关键设计细节 (1.1 - 1.5):**

| 你的要点 | 优化补充 | 对应概念 |
| :--- | :--- | :--- |
| **1.1 语义缓存** | 展示缓存时需同时展示**摘要、生成时间、相似度**，由学生决定是否使用。 | 语义缓存 + 人机协同 |
| **1.2 高级模型规划** | 提示词中需引用**产物契约** (JSON Schema)，确保输出可直接解析。 | 产物契约 |
| **1.3 持久化大纲** | 采用**版本化存储**：同一素材生成新大纲时，存入新版本，保留历史。 | 技能记忆 + 版本化 |
| **1.4 大纲结构设计** | 每道题需包含: `id`, `text`, `reference_answer`, `dimensions[]`, `weight`, `thinking_hint`, `timeout_seconds`, `preset_followups[]` | 产物契约 |
| **1.5 创建面试记录表** | 记录表中需**冗余存储** `plan_id` + `plan_version` + 岗位信息快照，防止后续大纲变更导致溯源失败。 | 可观测性 + 快照模式 |


### **阶段二：面试前数据准备 (上下文注入)**

> 对应你的第2点。

**2.1 获取面试大纲与进度**
- 根据 `session_id` 加载 `面试记录表`，从中获取当前的 `current_question_index` 和 `answers` 历史。
- 根据 `plan_id` + `plan_version` 加载完整的 `面试大纲`。

**2.2 获取用户画像**
- 根据 `user_id` 从持久化存储中加载 `用户画像`。
- **注入方式**: 将画像的关键信息（如“过往面试显示对数据库索引理解薄弱”）作为**知识锚点**，放入面试官 Agent 的 System Prompt 中。

```json
// 知识锚点注入示例
{
  "knowledge_anchor": {
    "user_weaknesses": ["数据库索引优化"],
    "user_strengths": ["Python异步编程"],
    "last_interview_summary": "上次面试在系统设计题上表现较好"
  }
}
```


### **阶段三：动态面试 (状态机驱动)**

> 对应你的第3点。这是系统的核心，建议使用 LangGraph 实现。

```mermaid
flowchart TD
    subgraph InterviewLoop [主面试循环 - 状态机]
        A[自我介绍环节] --> B[进入单题子状态机]
        B --> C{还有下一题？}
        C -- 是 --> B
        C -- 否 --> D[生成面试总结报告]
    end

    subgraph SingleRound [单题子状态机]
        direction TB
        S1[加载题目<br>+ thinking_hint] --> S2[调用面试官Agent<br>生成详细提问]
        S2 --> S3[启动计时器<br>等待回答]
        S3 --> S4{用户行为}

        S4 -- 正常回答 --> S5[Evaluator分析回答]
        S4 -- 提出疑问 --> S6[Clarifier澄清]
        S4 -- 请求提示 --> S7[提供思考提示<br>不提供答案]
        S4 -- 超时 --> S8[发送温和提示]

        S6 --> S3
        S7 --> S3
        S8 --> S3

        S5 --> S9{回答状态}
        S9 -- 完整 --> S10[Scorer记录评分<br>保存本题记录]
        S9 -- 不完整/偏离 --> S11[生成引导追问]
        S11 --> S3

        S8 -- 多次超时 --> S12[记录放弃本题]
        S12 --> S10
    end
```

**关键设计细节 (3.1 - 3.7):**

| 你的要点 | 优化补充 | 对应概念 |
| :--- | :--- | :--- |
| **3.1 自我介绍** | 作为独立的一题处理，拥有独立的评分维度和大纲引用。 | 扩展产物契约 |
| **3.2 题目深度挖掘** | 使用**ReAct模式**实现“提问-分析-追问”循环，直到 Evaluator 判定 `status=complete`。 | ReAct + 子状态机 |
| **3.2.1 意图处理** | 定义三种特殊意图: `ANSWER`、`CLARIFICATION`、`HINT_REQUEST`。`HINT_REQUEST` 只提供 `thinking_hint`，不提供答案。 | 产物契约 + 角色边界 |
| **3.2.2 结合业务场景** | 提示词中注入**岗位信息**和**简历关键点**，要求面试官 Agent 设计“场景题”。 | 知识锚点 |
| **3.3 超时与提示** | 超时逻辑**由代码控制**，而非LLM。LLM只负责在收到 `TIMEOUT` 事件后输出提示文本。 | 编码化约束 |
| **3.4 放弃本题** | 记录 `final_status=“abandoned”` 和 `timeout_count`，供后续反思使用。 | 可观测性 |
| **3.5 关闭窗口/放弃面试** | 捕获 `beforeunload` 事件，调用后端接口标记 `session.status=“abandoned”`，并**同步**生成简版总结。 | 状态管理 + 异常处理 |
| **3.6 重新面试** | 面试记录表中**快照存储**岗位信息、企业信息、简历快照。重新面试时，基于这些快照重新生成大纲或复用。 | 快照模式 |
| **3.7 面试总结报告** | 报告应结构化，包含: 各题得分、总分、评语、强项/弱项分析、面试官建议。 | 产物契约 |


### **阶段四：数据资产维护 (异步学习闭环)**

> 对应你的第4点。这是系统“越面试越好用”的核心引擎。

```mermaid
flowchart LR
    subgraph Trigger [触发源]
        A[面试结束<br>生成总结报告] --> B[推入任务队列]
    end

    subgraph AsyncTasks [后台异步处理]
        B --> C[Reflection Subagent]

        C --> D[更新用户画像<br>技术能力/沟通风格/薄弱点]
        C --> E[分析提问效果<br>哪些追问有效/哪些问题区分度低]
        C --> F[沉淀人群洞察<br>高频薄弱点/岗位匹配模式]
    end

    subgraph Assets [资产库更新]
        D --> G[(用户画像库)]
        E --> H[(面试大纲库<br>优化权重/标记低质量问题)]
        F --> I[(人群洞察库)]
    end

    I -.-> J[反馈给Planner<br>用于未来大纲生成]
```

**关键设计细节 (4.1 - 4.2):**

| 你的要点 | 优化补充 | 对应概念 |
| :--- | :--- | :--- |
| **4.1 维护答题记录** | `Reflection` 需分析: “对于问题X，当学生回答包含关键词Y后，追问Z是否有效？” 产出为**追问模式资产**。 | 技能记忆 |
| **4.1.1 提问经验** | 存储为结构化规则，例如: `{“trigger”: “answer_contains(‘缓存’)”, “followup”: “缓存击穿如何解决？”}` | 编码化约束 |
| **4.2 学生群体画像** | 产出应包含: `高频薄弱点`, `岗位匹配模式`, `易混淆概念`。这些资产可直接用于优化 `Planner` 的出题策略。 | 人群洞察资产 |


## 📊 与之前讨论的完整映射

| 你的大纲要点 | 映射的讨论概念 | 优化后的核心机制 |
| :--- | :--- | :--- |
| 1. 大纲缓存与生成 | 语义缓存 + 产物契约 + 技能记忆 | 缓存命中时用户确认；新大纲版本化存储 |
| 2. 面试前数据准备 | 三层记忆 + 锚点系统 | 加载大纲、进度、用户画像，注入知识锚点 |
| 3. 答题环节 | 状态机 + ReAct + 编码化约束 | 单题子状态机，超时由代码控制，意图分类处理 |
| 3.2 题目深度挖掘 | ReAct模式 | 提问-评估-追问循环，直到判定完整 |
| 3.3 超时与提示 | 编码化约束 + 护栏规则 | 超时逻辑由代码控制，LLM只负责生成提示文本 |
| 3.6 重新面试 | 快照模式 + 版本化 | 面试记录表快照岗位/简历信息 |
| 4. 数据资产维护 | Reflection + 异步总结 | 后台分析，产出用户画像、提问经验、人群洞察 |


## ✅ 下一步行动建议

1. **定义产物契约**: 先为 `InterviewPlan`, `InterviewSession`, `UserProfile`, `ReflectionReport` 定义完整的 Pydantic 模型或 JSON Schema。
2. **实现语义缓存**: 基于 Milvus 或类似向量数据库，实现大纲的存储与检索。
3. **搭建状态机骨架**: 使用 LangGraph 实现主状态机和单题子状态机的代码骨架。
4. **打磨第一个提示词**: 从 **面试官 Agent** 的基础角色定义开始，迭代优化。
5. **实现 Evaluator**: 这是单题循环的核心，决定何时完整、何时追问。

这份优化后的大纲，已经是一份可以直接指导开发的技术设计文档。你想从哪一个具体模块开始实现？









